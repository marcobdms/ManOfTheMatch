{
  "name": "manofthematch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n front,back -c blue,red \"npm run dev:front\" \"npm run dev:back\"",
    "dev:front": "npm --prefix frontend run dev",
    "dev:back": "npm --prefix backend run dev",
    "typecheck": "npm --prefix frontend run typecheck && npm --prefix backend run typecheck"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
