import RouteStub from '../components/RouteStub'

export default function Profile() {
  return (
    <RouteStub
      title="Perfil"
      note="Equipo favorito y ajustes de notificaciones (hoy hay partido, empieza pronto, goles)."
    />
  )
}
