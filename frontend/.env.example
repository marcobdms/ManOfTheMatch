# frontend — copy to .env.local (Vercel: set as project env vars, all "Production" + "Preview")
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Web Push (VAPID) — public key only; must match backend VAPID_PUBLIC_KEY.
# Generate a pair with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
